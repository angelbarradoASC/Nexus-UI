# =============================================================
# Nexus-UI Desktop — dependencias adicionales al app/requirements.txt
# =============================================================

# Ventana nativa (WebView2 de Windows, no bundlea Chromium)
pywebview==5.3.2

# System tray
pystray==0.19.5

# Procesamiento de imagenes (icono del tray)
Pillow==10.3.0

# Metricas del sistema local (CPU, RAM, disco, red, procesos)
psutil==5.9.8

# Windows API (COM objects: Outlook, Excel, Teams; Registry; Win32)
# Solo Windows — se instala solo si sys_platform == win32
pywin32==306

# WMI — hardware info avanzado (modelo CPU, GPU, BIOS, etc.)
# Solo Windows
wmi==1.5.1

# Portapapeles
pyperclip==1.8.2

# Shell de consola enriquecida
rich==13.7.1

# Agente de control de PC (PEPO desktop.system_task) — GUI, PowerShell,
# ficheros, vision por accesibilidad de Windows via LLM (proveedor OpenRouter
# reutilizado de llm_router.py).
#
# REQUISITO OBLIGATORIO DE SISTEMA — sin esto la instalacion falla:
#   Una dependencia transitiva (elevenlabs, usado solo para voz/STT-TTS que
#   AUN NO USAMOS pero que windows-use importa siempre al cargar el paquete)
#   tiene rutas de fichero mas largas que el limite de Windows (260
#   caracteres). Hay que activar "rutas largas" ANTES de instalar:
#
#   Desde PowerShell COMO ADMINISTRADOR (una sola vez, todo el sistema):
#     Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" `
#       -Name "LongPathsEnabled" -Value 1 -Type DWord
#
#   Sin este paso, `pip install windows-use` falla con:
#     OSError: [Errno 2] No such file or directory: '...\elevenlabs\...'
#   No hace falta reiniciar Windows, pip lo recoge en la siguiente ejecucion.
windows-use==0.8.1

# Automatizacion GUI (nivel 1+ permisos)
# pyautogui==0.9.54   # descomentar cuando se implemente

# Monitoreo de teclado/raton (nivel 1+ permisos)
# pynput==1.7.6       # descomentar cuando se implemente

# Teams (MSAL + Microsoft Graph)
msal==1.37.0
msal-extensions==1.3.1

# Empaquetado desarrollo/test
pyinstaller==6.3.0

# Credenciales seguras del proveedor desktop (Windows Credential Manager via keyring)
keyring==25.2.1

# Empaquetado produccion (obfuscacion)
# nuitka==2.1.0       # instalar aparte, requiere compilador C
