# =============================================================
# Nexus-UI Desktop — dependencias adicionales al app/requirements.txt
# =============================================================

# Ventana nativa (WebView2 de Windows, no bundlea Chromium)
pywebview==5.3.2

# System tray
pystray==0.19.5

# Procesamiento de imagenes (icono del tray)
Pillow==10.3.0

# Metricas del sistema local (CPU, RAM, disco, red, procesos)
psutil==5.9.8

# Windows API (COM objects: Outlook, Excel, Teams; Registry; Win32)
# Solo Windows — se instala solo si sys_platform == win32
pywin32==306

# WMI — hardware info avanzado (modelo CPU, GPU, BIOS, etc.)
# Solo Windows
wmi==1.5.1

# Portapapeles
pyperclip==1.8.2

# Shell de consola enriquecida
rich==13.7.1

# Agente de control de PC (PEPO desktop.system_task) — GUI, PowerShell,
# ficheros, vision por accesibilidad de Windows via LLM (proveedor OpenRouter
# reutilizado de llm_router.py).
#
# DESACTIVADO — no instalar hasta resolver esto:
#   windows-use 0.8.1 en PyPI arrastra una cola de ~15 SDKs sin relacion
#   (anthropic, cerebras, discord.py, elevenlabs, google-genai, litellm,
#   mistralai, ollama, openai, posthog, ipykernel...) con suelos de version
#   que chocan con el resto del proyecto (cryptography>=44, httpx>=0.28.1,
#   Pillow>=11.2.1, psutil>=7.0.0, pydantic>=2.11.7, pywin32>=311,
#   rich>=14.0.0 — subirlos todos SI resuelve el grafo, probado en vivo) y
#   ademas litellm en su version mas vieja compatible (1.93.0) no tiene
#   wheel para este Python/plataforma y solo compila via Rust/Cargo — el
#   auto-instalador de Rust de pip (puccinialin) fallo aqui con
#   "could not read metadata for file: ...rustup-init.exe".
#   _get_windows_use_agent() en system_task_agent.py ya hace el import de
#   forma perezosa (solo al usar el fallback de GUI), y de hecho NO esta
#   instalado en el venv de desarrollo que corre PEPO hoy — quitarlo de
#   aqui no rompe nada que ya funcione, solo deja sin ese fallback
#   concreto hasta que alguien lo revise con tiempo (fijar litellm a una
#   version con wheel, o sustituir windows-use por algo mas ligero).
# windows-use==0.8.1

# Automatizacion GUI (nivel 1+ permisos)
# pyautogui==0.9.54   # descomentar cuando se implemente

# Monitoreo de teclado/raton (nivel 1+ permisos)
# pynput==1.7.6       # descomentar cuando se implemente

# Teams (MSAL + Microsoft Graph)
msal==1.37.0
msal-extensions==1.3.1

# Empaquetado desarrollo/test
pyinstaller==6.3.0

# Credenciales seguras del proveedor desktop (Windows Credential Manager via keyring)
keyring==25.2.1

# Cliente MCP (Model Context Protocol) — permite a PEPO conectarse a
# servidores MCP externos (Slack, Notion, filesystem...) sin escribir una
# clase Python nueva por integracion. Ver app/nexus/mcp/manager.py.
#
# ATENCION AL INSTALAR/ACTUALIZAR — CONFLICTO REAL DE VERSIONES:
#   `pip install mcp` SIN pins explicitos arrastra el resolver de pip a
#   subir starlette a 1.x (rompe FastAPI 0.111.0, que exige
#   starlette<0.38.0 — confirmado en vivo: TypeError en Router.__init__,
#   'on_startup' ya no existe en starlette 1.x) y a subir pydantic/
#   pydantic-settings a versiones tambien mas nuevas de lo necesario.
#   mcp==1.6.0 SI funciona con las versiones que ya usa el resto del
#   proyecto (confirmado en vivo: conexion stdio real, list_tools +
#   call_tool correctos) — solo pydantic necesita un patch minimo
#   (2.7.1 -> 2.7.2, sin cambios de comportamiento; ese pin vivia
#   duplicado y desincronizado en app/requirements/base.txt, ya
#   corregido ahi tambien). Si se toca esta dependencia, instalar
#   SIEMPRE los 4 paquetes juntos en un solo comando para que el
#   resolver de pip no se vaya a versiones nuevas de
#   starlette/pydantic-settings sin necesidad real:
#     pip install mcp==1.6.0 starlette==0.37.2 httpx==0.27.0 pydantic==2.7.2 pydantic-settings==2.5.2
mcp==1.6.0
starlette==0.37.2
pydantic==2.7.2
pydantic-settings==2.5.2

# Empaquetado produccion (obfuscacion)
# nuitka==2.1.0       # instalar aparte, requiere compilador C
