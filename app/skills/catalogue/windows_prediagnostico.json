{
  "id": "windows.prediagnostico",
  "name": "Pre diagnostico Windows",
  "description": "Diagnostico sobre un servidor o servicio Windows REMOTO concreto (nombrado o identificable). Nunca uses esto para preguntas sobre 'esta máquina'/'mi ordenador'/el PC donde corre Nexus — eso es desktop.system_task.",
  "execution": {
    "where": "assistant",
    "timeout": 30,
    "retry": 0
  },
  "llm": {
    "required": true,
    "min_level": "L1",
    "preferred_level": "L1",
    "speed_level": "L1"
  },
  "inputs": {
    "target": {"type": "str", "required": false},
    "alert_hint": {"type": "str", "required": false}
  },
  "outputs": {
    "analysis": "str",
    "plan": "dict"
  },
  "triggers": [
    "alarma windows",
    "windows server",
    "winrm",
    "powershell",
    "iis",
    "event viewer"
  ],
  "examples": [
    "tengo una alarma en el windows app-win-02",
    "revisa el iis del windows server pagos-01"
  ]
}
