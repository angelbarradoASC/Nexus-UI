apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://192.168.68.150:9090
    isDefault: true
    editable: false

  # Loki no esta desplegado en el servidor recuperado (no aparece en docker ps) — se deja igual.
  - name: Loki
    type: loki
    access: proxy
    url: http://192.168.1.150:3100
    editable: false

  - name: AlertManager
    type: alertmanager
    access: proxy
    url: http://192.168.68.150:9094
    editable: false
