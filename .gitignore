# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml

# Virtual environments
.venv/
venv/
env/

# Binarios de terceros descargados en local (no son codigo fuente propio ni
# submodulo — cada plataforma/despliegue descarga el suyo, ver extractors.py)
vendor/obscura/

# Local environment
.env
.env.*
!.env.example
!.env.*.example

# Logs and runtime data
/logs/
*.log
/exports/
/release/
/tempo-data/
tmp-operator-*.png
*.zip
!tests/test_llm_parser.zip

# Local service data and generated observability payloads
mongodb/data/
redis/data/
grafana/data/
app/data/

# Historical and audit dumps
/AUDIT/
/OLD/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/

# Desktop build outputs (build/OpenNexus.spec is source, keep it tracked)
build/.venv-open-nexus/
build/OpenNexus/
dist/
*.spec
!build/OpenNexus.spec

# Node / frontend cache if introduced later
node_modules/

# Local graphify analysis output
/graphify-out/

# Local persistence and mutable runtime state
/data/
/runtime/
/backups/
*.sqlite
*.sqlite3
*.db
*.db-journal
*.db-wal
*.db-shm
