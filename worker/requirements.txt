# =============================================================
# worker/requirements.txt — dependencias del worker Redis
# Standalone (build context Docker es ./worker, no puede acceder
# a ../app/requirements/). Mantener sincronizado con base.txt.
# =============================================================

# Config
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.3.0

# Database
pymongo==4.8.0
dnspython==2.6.0
redis==5.0.1

# HTTP client (LLM calls, Jira API, web fetch)
httpx==0.27.0

# Web search & scraping (WebAgent)
duckduckgo-search==6.3.7
beautifulsoup4==4.12.3
lxml==5.2.2

# SSH (SSHAgent)
paramiko==3.4.0

# Encryption (CredentialStore)
cryptography==41.0.7

# Auth (session_auth importado por config)
bcrypt==4.1.3

# Logging
loguru==0.7.2

# Observability
prometheus-client==0.21.0
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
